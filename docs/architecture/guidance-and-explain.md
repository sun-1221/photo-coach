# 口令引擎与讲解 API 专题

- 状态：当前架构规范附件
- 权威入口：[../architecture.md](../architecture.md)
- 产品约束：[产品需求总纲](../requirement.md)
- 原始位置：重构前架构第 4.3、4.4 节
- 迁移方式：Moved；线程模型、节奏、TTS、API 同意与失败隔离完整保留

跨场景短口令数量按 CP-04 决议由意图、场景和安全回退覆盖推导；历史 8–12 与 12–16 区间均不作为验收配额。详见[需求决策与冲突记录](../traceability/decisions-and-conflicts.md)。

---

### 4.3 口令引擎

- 输入：上述信号
- 输出：最多 3 条人话，带听众字段
- 语音播报当前稳定的 `shooter` 或 `subject` 口令；拍摄者动作卡始终可见，中文语音和被拍者字幕作为两个独立、持久化的输出开关
- Android 11+ 清单声明 `TTS_SERVICE` 可见性；初始化优先选已安装的简体中文离线音色，OEM 引擎不提供音色枚举时回退 `setLanguage(zh-CN)`，仍不新增麦克风或网络依赖
- CameraX / ML Kit 回调只把最新分析帧投入容量为一的事件流；`GuidanceSession`、100ms tick、UI 状态与 TTS 回调统一在主线程 reducer 中串行处理，避免真机多线程竞态和旧帧积压
- Android 每约 300ms 将最新信号送入口令引擎；节奏器按 600ms/连续三帧防抖更新当前动作槽，动作完成、失效或被稳定的新候选替换后不得继续保留旧口令
- 所有指导结束事件统一消费交互规格第 6.2 节判定；TTS 完成、超时及通道关闭不能直接赋予可拍状态。稳定候选到界面显示适用 ≤300ms p95，完整画面响应另报且包含防抖。
- Ready 继续按相同稳定门槛刷新最低质量判断及当前未满足原因。两步/可选预算用完不抹去质量原因；原因只占当前动作说明，不增加编号步骤或重复 TTS。已有暗脸和人物高速运动信号成立时不得确认最低质量；不新增/放宽检测阈值。
- “请露出脸”与镜头遮挡属于持续恢复口令：问题解决后可回到 Ready，但后续稳定复发时必须再次进入动作卡；重新出现不消耗新的两步预算，也不得因曾展示过而静默。
- 姿势只匹配短口令核 + 当前场景特有句，不维护男/女姿势大库
- JSON 进仓库，用单测锁回归样张（需求 11.3）和核口令（需求 11.5）

### 4.4 讲解 API

- 默认关；用户点「再讲细」且已单独同意后才上传当前帧（短边 512–768）
- 请求带上端侧已算信号和已提示的 3 条
- 返回固定 JSON：最多 3 条指令 + 一句原因，每条带听众；缺听众或术语（光圈/ISO）客户端丢掉
- 供应商可替换
- 主路径不依赖本 API，飞行模式取景器仍可用

2026-09-12：稳定计数只消费捕获时刻不同且递增的新鲜帧；旧会话、重复快照、乱序结果丢弃。TTS utterance与指导会话/动作代次关联，替换/关闭/面板打开后停止旧句，迟到完成回调不推进新动作。中文离线音色缺失时按当前字幕设置给可见降级，不新增下载依赖或暗开字幕；新安装断网须独立验收。工程代码符合性及新分支验证仍为 Unknown/NotRun。
