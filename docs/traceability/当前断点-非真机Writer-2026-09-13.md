# 当前断点：非真机唯一 Writer（2026-09-13，78j 后）

新对话只要用户说「继续」，先读本文件，再读 Skill 与两个入口。不要从 B3/C3、266 JVM、历史 APK SHA 或 2f 轮摘要起算。

- 身份：同一本地唯一 Writer；总控 `01a0949d-eddb-7240-b1fc-ecb9b1a0d3b1`。上一写入对话：[继续](cac4d572-d849-4ac5-840a-8b692e04a646)。
- 目录：`E:\Code\Agent\photo-coach`。不建 worktree，不提交/推送（除非用户本轮明确要求），不升级依赖，不操作真机，不作产品 Go。
- 产品权威：`docs/requirement.md` → `docs/architecture.md`，再按任务读附件。执行 Skill：`.agents/skills/photo-coach-coding`。
- 状态入口：[总控验收](completion-2026-09-12.md) / [执行流水](non-device-acceptance-2026-09-12.md) / [用例索引](non-device-test-index-2026-09-12.md) / [Reader 原文](non-device-reader-research-2026-09-12.md) / [总控阶段](controller-workflow-2026-09-12.md)。
- 源码事实：当前有效实现在本 checkout **未提交工作区**，不是 B3 冻结树。换对话不换目录即可接着改；换机器或干净 clone 会丢未提交改动。

## 范围

- P-1 `InScope` + 已批准 P1 `ApprovedSeparate`。P0 `GateLocked`，后期 `Deferred`。
- Live 为方案 C：CameraX **1.6.1**、四用例、公开 `VIDEO_CAPTURE` effect、双编码器。窗口仍是快门前后各约 **1.5 秒**，媒体跨度 **8 秒**。
- 无 `ConflictPending`。封面时间容差未冻结。

## 环境（专用 AVD，不要开第二台）

```
ANDROID_HOME / ANDROID_SDK_ROOT = D:\DevEnvironment\Android\SDK
ANDROID_SERIAL = emulator-5580
ANDROID_ADB_SERVER_PORT = 5038
GRADLE_USER_HOME = C:\Users\19744\.gradle
```

- 指纹：`google/sdk_gphone64_x86_64/emu64xa:16/BE4B.251210.005/14574095:user/release-keys`
- 1080×2400。动画保持历史 0，不要改。
- 离线构建必须用本机 `.gradle`。若环境把 `GRADLE_USER_HOME` 指到 `cursor-sandbox-cache\...\gradle`，插件会找不到，先改回再 `--offline`。
- 报告目录：`androidApp/build/reports/non-device-2026-09-12/`。新报告用中文名，下一轮从 **79** 起，例如 `第79轮-实际Live-SPEED-….txt`。不要叫 77。
- 设备证据：`/sdcard/Android/data/com.photocoach.app/files/acceptance-evidence/`。78j 当时仍是英文名；测试源码已改为中文名，尚未用新名复跑。

## 已关闭（只表示所列用例，不是整项产品验收）

| 项 | 结果 | 报告 |
|---|---|---|
| Reader：放弃标记首次写盘失败 | 61 Pass | `target-61.txt` |
| Reader：权限设置返回拍照 | 67 Pass | `permission-runtime-67.txt` |
| Reader：实际编辑两轮 | **78j Pass** | [第78j轮-编辑两轮-通过.txt](../../androidApp/build/reports/non-device-2026-09-12/第78j轮-编辑两轮-通过.txt) |
| 合成双 MP4 五变换、55/57 套件、研究变体等 | 历史 Pass，有重叠不相加 | `final-instrumented-55.txt` / `target-57.txt` 等英文历史名 |

## 未关闭

| 项 | 当前事实 | 下一对话怎么做 |
|---|---|---|
| 实际 Live 正向成片 | 78j SPEED：`hasPrebuffer=true`，`awaitingCoverage` 时 8s 空闲未切断，自有 20 样本 mux 通过；JPEG `10642331073000`，retained 末 `10643297849800`，后覆盖约 0.967s，短约 0.533s；媒体跨度上限停录；**正确 JPEG 回退** | 见下方「若用户说继续」 |
| 完整 Coach / Android JVM | 72 轮离线 128+219 为当时数；78j 只定向跑过时间线/预算/完成分发/所有权/编辑历史 | 只有改到这些模块外才重跑整套 |
| 放弃 / 权限复跑 | 61/67 通过后源码未再为这两项改生产逻辑；测试证据文件名已改中文 | 非必要不重跑冒充新 Pass |
| 目标机 / HyperOS / 颜色 / 热量 / 封面容差 / 研究 Go | NotRun / Unknown | 不把 AVD 写成小米 14 Pro |

## 若用户说「继续」

1. 读本文件、Skill、`docs/requirement.md`、`docs/architecture.md`，Live/编辑再读 P1 创意 + 创意存储 + 相机感知。
2. 核对 `emulator-5580` / adb `5038` 仍在线。不要再启第二台模拟器。
3. **不要**再磨编辑无障碍，除非 78j 布局被改坏。
4. Live：只修「不放宽 1.5s/8s、不改 mux/sensor 精确关联」仍能成立的生产缺口。禁止把剩余门槛改成 3s，禁止 LIGHT 热注入冒充 SD 成片，禁止 JPEG 回退写成 Pass。
5. 78j 已证明：快门时剩余约 1.5s 挡不住晚到 JPEG + 1.5s 后覆盖。这是已记录剩余风险，不是未发现的空锚点。没有新机制就不要空转同一 SPEED 用例。
6. 新宿主报告用中文名；改执行流水与本断点，不要回写 B3 段。
7. 用户未要求则不提交、不推送、不宣布非真机验收完成、不启动 Reader。

## 当前关键源码（工作区）

- 编辑面板：`CreativeResultPanel.kt`（`fillMaxHeight`、预览 140.dp、七滑块在 `weight` 滚动区、页脚在滚动区外）。
- 编辑仪器：`ActivityLifecycleAcceptanceTest` 打开后记 `initialEdit`；页脚用 `ACTION_CLICK` / 子节点 / 屏幕坐标；证据文件已改中文名。
- Live：`MotionFrameTimeline.hasPrebuffer()` 要关联后缀 ≥1.5s **且** 剩余媒体跨度 ≥1.5s；快门立即 `holdForCoverage`；空闲 8s 不切断在途覆盖。
- 定向 JVM：`prebufferIsNotReadyWhenRemainingMediaSpanCannotHoldPostbuffer`。

## 硬约束

- CameraX 固定 1.6.1；不用私有 / 反射 API。
- 不放宽 mux/sensor、1.5s 窗口、8s 上限、容器校验。
- 不把 JVM Pass 写成仪器/真机 Pass；不把 AVD 当目标机。
- Gradle 离线；`verifyDebugManifestPolicy` 须仍为仅 CAMERA。

## 跑法备忘

```
.\gradlew.bat :androidApp:assembleDebug :androidApp:assembleDebugAndroidTest --offline
adb -P 5038 -s emulator-5580 install -r -t androidApp\build\outputs\apk\debug\androidApp-debug.apk
adb -P 5038 -s emulator-5580 install -r -t androidApp\build\outputs\apk\androidTest\debug\androidApp-debug-androidTest.apk
adb -P 5038 -s emulator-5580 shell am force-stop com.photocoach.app
```

Live（仅当有新的不放宽窗口的修复时）：

```
adb -P 5038 -s emulator-5580 shell am instrument -w -r -e liveCapturePriority SPEED -e class com.photocoach.app.ActivityLifecycleAcceptanceTest#actualLiveWithMeasuredPrebufferPublishesVerifiedMotionPhoto com.photocoach.app.test/androidx.test.runner.AndroidJUnitRunner
```

完成汇报仍用 Skill 的 Changed / Scope / Delivery / Verification / Rules / Validated / NotRun / Residual risk。
